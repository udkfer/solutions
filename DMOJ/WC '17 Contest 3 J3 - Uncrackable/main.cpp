#include <iostream>
#include <string>

int main()
{
  std::string password{};
  std::cin >> password;

  //if((password.size() >= 8) && (password.size <= 12) && (password has at least 3 lowercase letters) && (password has at least 2 uppercase letters) && (password has at least 1 number))
  //std::count << "Valid\n";
  //else
  //std::count << "Invalid\n";

  return 0;
}
